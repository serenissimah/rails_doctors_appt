<h1>Doctors</h1>
<hr />
<h2><%= @doctors.length <= 0 ? "No Doctors available" : "" %></h2> 
<ul>
  <% @doctors.each do |doctor| %>
    <li>
      <%= link_to doctor.name, doctor_path(doctor) %>
    </li>
<% end %>
</ul>